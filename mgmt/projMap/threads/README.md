# Thread Bootstrap

Use `resolve-init-thread.ps1` to resolve latest rollout thread from `${SESSIONS_ROOT}`.

`current-thread.json` stores latest resolved thread.
`all-threads.json` stores all resolved project threads.
