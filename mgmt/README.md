# combiLog mgmt

Thread discovery/cache lives in `mgmt\projMap\threads\`.
`current-thread.json` stores latest resolved thread.
`all-threads.json` stores all resolved project threads.
`projMap\project.txt` stores monolithic plain-English project spec with fenced front matter blocks.
